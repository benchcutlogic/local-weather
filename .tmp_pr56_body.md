## Summary
- switch frontend CI install/build from npm to pnpm (repo standard)
- deploy Cloudflare Pages from SvelteKit adapter output (`.svelte-kit/cloudflare`) instead of missing `dist`

## Why
`Build & Deploy` on `main` is failing at Pages deploy with:
- `ENOENT ... frontend/dist`

SvelteKit + `@sveltejs/adapter-cloudflare` emits build artifacts under `.svelte-kit/cloudflare`, not `dist`.

## Validation
- workflow file updated for pnpm + correct output path
